@include `allocator`;
@include `data-serializer`;
@include `render-graph`;

@fn puleAssetRenderGraphFromPds(
  allocator : PuleAllocator,
  platform : PulePlatform,
  renderGraphValue : PuleDsValue
) PuleRenderGraph;
